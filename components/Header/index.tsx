export const Header = () => {
  return (
    <div
      className={`w-full h-14 bg-white fixed px-5 flex items-center
      shadow-header text-lg font-semibold`}
    >
      تکمیل اطلاعات
    </div>
  )
}
